<section class="relative min-h-[92vh] sm:min-h-[85vh] lg:min-h-[90vh] flex items-center pt-24 pb-12 sm:py-16 md:py-20 px-5 sm:px-10 lg:px-16 overflow-hidden bg-navy-darker text-canvas">
  <!-- ========================================================================= -->
  <!-- HERO BACKGROUND VIDEO (MOBILE OPTIMIZED: OBJECT POSITIONING & AUTOPLAY) -->
  <!-- ========================================================================= -->
  <div class="absolute inset-0 z-0 overflow-hidden pointer-events-none">
    <video
      id="hero-bg-video"
      autoplay
      loop
      muted
      playsinline
      webkit-playsinline="true"
      disablepictureinpicture
      preload="auto"
      poster="/images/hero_main.png"
      class="w-full h-full object-cover object-[55%_center] sm:object-center transition-opacity duration-700 [image-rendering:-webkit-optimize-contrast]"
    >
      <source src="/videos/hero-Section.mp4" type="video/mp4" />
      <source src="/videos/hero_bg.mp4" type="video/mp4" />
    </video>
  </div>

  <!-- Foreground Content Container -->
  <div class="max-w-7xl mx-auto w-full relative z-10">
    <div class="max-w-3xl" data-reveal="fade-up">
      <!-- High-Visibility Eyebrow Badge (Sharp Contrast Across All Video Frames) -->
      <div class="inline-flex items-center gap-2.5 px-4 py-1.5 rounded-full backdrop-blur-md bg-navy-darker/70 border border-white/30 text-white shadow-md mb-4 sm:mb-5">
        <span class="w-2 h-2 rounded-full bg-emerald-400 shadow-[0_0_8px_rgba(52,211,153,0.8)]"></span>
        <span class="font-mono text-[11px] sm:text-xs font-semibold uppercase tracking-wider text-white [text-shadow:_0_1px_3px_rgba(0,0,0,0.9)]">
          Trusted UAE Specialists • Specialized Project Execution
        </span>
      </div>

      <!-- Main Headline: High-Contrast White with Gold Accent (Unchanged) -->
      <h1 class="font-display font-extrabold text-3xl sm:text-4xl md:text-5xl lg:text-[54px] leading-[1.14] tracking-tight text-white mb-4 sm:mb-5 [text-shadow:_0_2px_4px_rgba(0,0,0,0.95),_0_4px_20px_rgba(0,0,0,0.9)]">
        Architectural <span class="text-gold-glow drop-shadow-[0_2px_10px_rgba(0,0,0,0.85)]">Tensile Shades</span>, Tents & Steel Fabrication
      </h1>

      <!-- Professional Subheading (Formatted into 3 Balanced Lines with Medium-Large Font) -->
      <p class="font-body text-white/95 text-[15px] sm:text-[17px] md:text-[18px] leading-[1.65] font-normal mb-5 sm:mb-6 max-w-lg sm:max-w-xl [text-shadow:_0_1px_2px_rgba(0,0,0,0.95),_0_2px_8px_rgba(0,0,0,0.9),_0_0_16px_rgba(0,0,0,0.75)]">
        Delivering custom shade systems, durable enclosures, and structural metalwork with certified precision and end-to-end professional support across the UAE.
      </p>

      <!-- Simple 3 Separate Trust Points (Not Connected) -->
      <div class="flex flex-wrap items-center gap-2 sm:gap-2.5 mb-6 sm:mb-7">
        <div class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg backdrop-blur-md bg-navy-darker/70 border border-white/30 text-xs sm:text-[12.5px] font-semibold text-white shadow-sm">
          <span class="text-emerald-400 font-bold text-sm">✓</span>
          <span class="[text-shadow:_0_1px_3px_rgba(0,0,0,0.9)]">500+ Clients</span>
        </div>

        <div class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg backdrop-blur-md bg-navy-darker/70 border border-white/30 text-xs sm:text-[12.5px] font-semibold text-white shadow-sm">
          <span class="text-emerald-400 font-bold text-sm">✓</span>
          <span class="[text-shadow:_0_1px_3px_rgba(0,0,0,0.9)]">Certified Quality</span>
        </div>

        <div class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg backdrop-blur-md bg-navy-darker/70 border border-white/30 text-xs sm:text-[12.5px] font-semibold text-white shadow-sm">
          <span class="text-emerald-400 font-bold text-sm">✓</span>
          <span class="[text-shadow:_0_1px_3px_rgba(0,0,0,0.9)]">Fast Handover</span>
        </div>
      </div>

      <!-- Solid Brand Green & Solid White Buttons (Live Color Changing & Interactive Hover Effects) -->
      <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-3 sm:gap-3.5">
        <!-- Solid Green Primary Button with High-Contrast Color Flip, Glow Aura & Light Shimmer -->
        <a 
          href="/#contact" 
          class="btn-live-primary btn-shimmer w-full sm:w-auto font-body font-semibold text-xs sm:text-sm px-6 py-2.5 sm:py-3 rounded-lg sm:rounded-xl flex items-center justify-center gap-2.5"
        >
          <span class="relative z-10">Request a Free Quote</span>
          <svg class="relative z-10 w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
        </a>

        <!-- Solid Pure White Secondary Button with Color Inversion to Green on Hover -->
        <a 
          href="/projects" 
          class="btn-live-secondary btn-shimmer w-full sm:w-auto font-body font-semibold text-xs sm:text-sm px-6 py-2.5 sm:py-3 rounded-lg sm:rounded-xl flex items-center justify-center gap-2"
        >
          <span class="relative z-10">Explore Projects</span>
          <svg class="relative z-10 w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- Bottom Floating Video Control Pill -->
  <div class="absolute bottom-4 right-4 sm:bottom-5 sm:right-6 z-20 flex items-center gap-1.5 sm:gap-2 bg-navy-darker/80 backdrop-blur-md px-2.5 sm:px-3 py-1 sm:py-1.5 rounded-full border border-white/10 text-[10px] sm:text-[11px] font-mono text-white/80 shadow-lg">
    <span class="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse"></span>
    <span class="hidden xs:inline">Facility Reel</span>
    <button 
      id="video-toggle-btn"
      type="button" 
      class="ml-0.5 sm:ml-1 hover:text-white transition-colors p-1"
      aria-label="Pause or Play Background Video"
      title="Pause / Play Video"
    >
      <svg id="video-pause-icon" class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <svg id="video-play-icon" class="w-3.5 h-3.5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
    </button>
  </div>
</section>

<!-- Hero Video Interactive Controller Script with Mobile Autoplay Assurance -->
<script>
  function initHeroVideo() {
    const video = document.getElementById('hero-bg-video') as HTMLVideoElement;
    const toggleBtn = document.getElementById('video-toggle-btn');
    const pauseIcon = document.getElementById('video-pause-icon');
    const playIcon = document.getElementById('video-play-icon');

    if (!video) return;

    // Mobile Autoplay Assurance (iOS Low-Power mode / gesture unlock)
    const playPromise = video.play();
    if (playPromise !== undefined) {
      playPromise.catch(() => {
        // Auto-play was prevented by mobile browser policies; play on first interaction
        const startPlayOnInteraction = () => {
          video.play().catch(() => {});
          document.removeEventListener('touchstart', startPlayOnInteraction);
          document.removeEventListener('scroll', startPlayOnInteraction);
        };
        document.addEventListener('touchstart', startPlayOnInteraction, { passive: true, once: true });
        document.addEventListener('scroll', startPlayOnInteraction, { passive: true, once: true });
      });
    }

    if (toggleBtn) {
      toggleBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        if (video.paused) {
          video.play();
          pauseIcon?.classList.remove('hidden');
          playIcon?.classList.add('hidden');
        } else {
          video.pause();
          pauseIcon?.classList.add('hidden');
          playIcon?.classList.remove('hidden');
        }
      });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initHeroVideo);
  } else {
    initHeroVideo();
  }
</script>

